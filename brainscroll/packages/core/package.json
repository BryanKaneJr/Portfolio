{
  "name": "@brainscroll/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Frozen product rules, stable ID format, content schema and pure progression logic shared by app, scripts and admin.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^5.0.1"
  }
}
