/**
 * @cc/ui — shared React presentation components built on shadcn/ui + Tailwind.
 * Houses cross-app primitives (CitationBadge, SourceStatus, AnswerCard, etc.).
 * Components are added as the web app needs them — see docs/BACKLOG.md epic CC-UI.
 * Kept free of data-fetching logic so components stay pure and Storybook-able.
 */
export const UI_PACKAGE = "@cc/ui";
